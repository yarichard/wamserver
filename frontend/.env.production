# Production Environment Variables

# WebSocket Host - Set your production domain here
# Example: VITE_WS_HOST=wamserver.example.com
# If not set, will use window.location.host (recommended for same-domain deployment)
VITE_WS_HOST=localhost:3000

# API Base URL - Set if your API is on a different domain
# VITE_API_BASE_URL=https://api.example.com
