# Development Environment Variables

# WebSocket Host (defaults to window.location.host if not set)
VITE_WS_HOST=localhost:3000

# API Base URL (defaults to empty string - relative URLs)
# VITE_API_BASE_URL=http://localhost:3000
